# Extraction Spec

Framework: `campana_kobayashi_hyperbolicity`. Constants are extracted from `artifacts/constants_extraction_inputs.json` and promoted into the registry before the closure guard runs.
